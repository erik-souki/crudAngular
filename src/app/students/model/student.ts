export interface Student {
  _id: string;
  name: string;
  ra: string;
  //ficar de olho aqui, pq ra é do tipo long, mas coloquei string para não dar erro
  //quando tiver terminando analisar isso aqui
}
